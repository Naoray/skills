Inherits skill-creator/evals/checks.md (C1-C15).

## Skill-specific checks

C16. **JSON for parseable Scribe commands** - Any command whose output will be parsed must include `--json`. Why: styled CLI output is not stable for agents.
