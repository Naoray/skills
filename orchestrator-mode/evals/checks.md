Inherits skill-creator/evals/checks.md (C1–C15).

## Skill-specific checks

C16. **Flags router follow-up** — skill body remains unsplit but reports that orchestrator-mode is large enough for future router extraction. Why: assigned batch explicitly requires a follow-up flag instead of a split.
